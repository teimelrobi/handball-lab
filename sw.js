const CACHE_VERSION='handball-lab-v4.2.3';const APP_CACHE='app-'+CACHE_VERSION;const RUNTIME_CACHE='runtime-'+CACHE_VERSION;const CORE=['./','./index.html','./manifest.webmanifest','./icons/icon-192.png','./icons/icon-512.png','./icons/icon-maskable-512.png','./icons/apple-touch-icon.png'];self.addEventListener('install',e=>e.waitUntil(caches.open(APP_CACHE).then(c=>c.addAll(CORE))));self.addEventListener('activate',e=>e.waitUntil((async()=>{const keys=await caches.keys();await Promise.all(keys.filter(k=>k!==APP_CACHE&&k!==RUNTIME_CACHE).map(k=>caches.delete(k)));await self.clients.claim();})()));self.addEventListener('message',e=>{if(e.data==='SKIP_WAITING')self.skipWaiting();});self.addEventListener('fetch',e=>{const r=e.request;if(r.method!=='GET')return;const u=new URL(r.url);if(u.hostname.endsWith('.supabase.co')||u.protocol==='blob:')return;if(r.mode==='navigate'){e.respondWith((async()=>{try{const fresh=await fetch(r);const c=await caches.open(APP_CACHE);c.put('./index.html',fresh.clone());return fresh;}catch{return(await caches.match(r))||(await caches.match('./index.html'))||(await caches.match('./'));}})());return;}const cdn=u.hostname==='cdn.jsdelivr.net'||u.hostname==='cdnjs.cloudflare.com';if(u.origin===self.location.origin||cdn)e.respondWith((async()=>{const hit=await caches.match(r);const net=fetch(r).then(async x=>{if(x&&(x.ok||x.type==='opaque')){const c=await caches.open(u.origin===self.location.origin?APP_CACHE:RUNTIME_CACHE);c.put(r,x.clone());}return x;}).catch(()=>null);return hit||(await net)||Response.error();})());});